" Set tab to use actual tab characters, not spaces
set noexpandtab   " Use tabs instead of spaces
set tabstop=4     " Number of spaces a <Tab> counts for (visual width)
set shiftwidth=4  " Number of spaces used for autoindent
set softtabstop=4 " Makes Tab key behave like it's 4 spaces, even if tabs are used
